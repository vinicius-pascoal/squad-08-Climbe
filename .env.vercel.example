# Example environment for Vercel deployment (configure in Vercel Dashboard)
# Copy values into Vercel Project Settings > Environment Variables.
# Do not commit real secrets.

# --- Database (use a managed MySQL like PlanetScale/AWS RDS) ---
DATABASE_URL=mysql://USER:PASSWORD@HOST:3306/DBNAME

# --- Auth / JWT ---
JWT_SECRET=change-me
JWT_EXPIRES_IN=30m
BCRYPT_ROUNDS=10

# --- SMTP (optional; for email features) ---
SMTP_ENABLED=false
SMTP_HOST=
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# --- OAuth Google (optional; for Google login) ---
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# --- Origins (optional; defaults derive from VERCEL_URL) ---
# Set only if you use a custom domain and want to be explicit:
# FRONTEND_ORIGIN=https://your-domain.com
# BACKEND_ORIGIN=https://your-domain.com

# --- Frontend (Vite) ---
VITE_API_BASE=/api
# Leave empty to use same origin (recommended on Vercel)
VITE_BACKEND_URI=
