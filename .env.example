# =============================================================================
# VARIÁVEIS DE AMBIENTE DA APLICAÇÃO CLIMBE
# =============================================================================
# Copie este arquivo para .env e atualize os valores de acordo com seu ambiente
#
# IMPORTANTE: Nunca faça commit do arquivo .env real para controle de versão!
# =============================================================================

# -----------------------------------------------------------------------------
# CONFIGURAÇÃO DO BANCO DE DADOS
# -----------------------------------------------------------------------------

# Senha root do MySQL (usada pelo container MySQL)
MYSQL_ROOT_PASSWORD=root

# Nome do banco de dados da aplicação
MYSQL_DATABASE=appdb

# Usuário do banco de dados da aplicação
MYSQL_USER=app

# Senha do banco de dados da aplicação
MYSQL_PASSWORD=app

# Porta do banco de dados (exposta na máquina host)
# Padrão: 3306
DB_PORT=3306

# URL de conexão do banco para Prisma
# Formato: mysql://usuario:senha@host:porta/banco
# Para Docker: use 'db' como host (nome do serviço no docker-compose.yml)
# Para desenvolvimento local: use 'localhost' como host
DATABASE_URL=mysql://app:app@db:3306/appdb

# -----------------------------------------------------------------------------
# CONFIGURAÇÃO DA APLICAÇÃO
# -----------------------------------------------------------------------------

# Ambiente Node.js
# Opções: development, production, test
NODE_ENV=development

# Porta da aplicação (servidor backend)
# Padrão: 3000
PORT=3000

# -----------------------------------------------------------------------------
# CONFIGURAÇÃO DE DESENVOLVIMENTO
# -----------------------------------------------------------------------------

# Habilitar monitoramento de arquivos com polling (útil para Docker no Windows/macOS)
CHOKIDAR_USEPOLLING=true
WATCHPACK_POLLING=true

# Forçar Hot Module Replacement para Vite (frontend)
VITE_FORCE_HMR=true

# -----------------------------------------------------------------------------
# CONFIGURAÇÃO DE PRODUÇÃO
# -----------------------------------------------------------------------------

# Em produção, você pode querer sobrescrever alguns valores:
# DATABASE_URL=mysql://usuario_prod:senha_segura@host_prod:3306/banco_prod
# NODE_ENV=production
# PORT=3000

# -----------------------------------------------------------------------------
# CONFIGURAÇÃO OPCIONAL
# -----------------------------------------------------------------------------

# Adicione aqui quaisquer variáveis de ambiente adicionais que sua aplicação precise
# Exemplos:
# JWT_SECRET=sua-chave-secreta-jwt
# CORS_ORIGIN=https://seudominio.com
# LOG_LEVEL=info
# UPLOAD_MAX_SIZE=10mb
