{
  "name": "climbe",
  "private": true,
  "packageManager": "npm@10.8.1",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "devDependencies": {
    "turbo": "^2.5.6"
  },
  "dependencies": {
    "googleapis": "^159.0.0"
  }
}
